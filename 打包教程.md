# TwT 安装打包注意事项教程

本文档记录了将 Mineradio 二创版改名为 TwT 并打包成 Windows 安装包的全过程，以及踩过的所有坑。

## 一、环境准备

### 1.1 Node.js 依赖安装

```bash
npm install
```

**坑 1：Electron 二进制下载超时**

`npm install` 只下载 electron 的 npm 包（JS wrapper），不下载实际二进制。运行时或打包时才下载二进制，国内网络几乎必超时。

**解决：** 设置国内镜像后手动下载解压。

```bash
# 设置环境变量
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"

# 手动下载 electron v42.4.1 的 zip
# 下载地址：https://npmmirror.com/mirrors/electron/42.4.1/electron-v42.4.1-win32-x64.zip
# 解压到 node_modules/electron/dist/
```

### 1.2 electron-builder 工具包缓存

**坑 2：electron-builder 工具包下载超时**

electron-builder 打包时需要 NSIS、7zip、win-codesign 等工具包，这些从 GitHub 下载，国内极易超时。

**解决：** 手动下载工具包解压到缓存目录，并写 `.state` 文件标记完成。

缓存目录结构：
```
%LOCALAPPDATA%\electron-builder\Cache\
├── nsis\nsis-3.0.4.1\        # NSIS 编译器
├── nsis\nsis-resources-3.4.1\ # NSIS 资源
├── 7zip@1.0.0\               # 7zip
└── winCodeSign@1.1.0\        # 代码签名工具（不签名也要下载）
```

**坑 3：缓存目录必须写 .state 文件**

仅解压工具包到缓存目录是不够的，electron-builder 不认。必须在解压目录旁创建 `.state` 文件：

```
<解压目录>.state
```

内容示例：
```json
{"fileCount": 123}
```

`.state` 文件路径是 `<extractDir>.state`（在 extractDir 路径后加 `.state` 后缀，不是目录里面的文件）。

**坑 4：folderName 含 djb2 hash 后缀**

缓存子目录名包含 URL 的 djb2 hash（前5位），不能随便命名。格式为 `<releaseName>-<hash>`。如果目录名不对，electron-builder 会重新下载。

**更简单的方案：** 设置镜像环境变量让 electron-builder 自动下载：

```bash
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"
```

## 二、改名与品牌替换

### 2.1 package.json 配置

```json
{
  "name": "twt",
  "productName": "TwT",
  "build": {
    "appId": "com.twt.desktop",
    "productName": "TwT",
    "win": {
      "executableName": "TwT",
      "icon": "build/icon.ico",
      "signAndEditExecutable": true,
      "signExecutable": false
    },
    "nsis": {
      "shortcutName": "TwT",
      "installerIcon": "build/icon.ico",
      "uninstallerIcon": "build/icon.ico",
      "artifactName": "TwT-${version}-Setup.${ext}"
    }
  }
}
```

**坑 5：signAndEditExecutable vs signExecutable**

- `signAndEditExecutable: false` → 跳过 exe 图标注入 AND 签名。exe 图标不会改！
- `signAndEditExecutable: true` + `signExecutable: false` → 保留图标注入，跳过签名。✅ 正确方案

### 2.2 源码中的显示文字

需修改的文件：
- `desktop/main.js` — 窗口标题、托盘菜单、对话框文字、APP_NAME 常量
- `public/index.html` — 网页 title、开屏 logo、各种 UI 文字
- `server.js` — 后端 API、日志文字
- `public/wallpaper.html`、`public/desktop-lyrics.html` — title

**坑 6：不要全局替换 "mineradio" → "twt"**

源码中有很多 `mineradio-` 前缀的内部标识符（如 IPC 通道名 `mineradio-tray-action`、命令行参数 `--mineradio-user-data-dir`、partition 名 `persist:mineradio-netease-login`）。这些是内部通信标识符，改了会导致功能断裂。只改**用户可见的显示文字**。

### 2.3 NSIS 安装脚本 (build/installer.nsh)

**坑 7：installer.nsh 中 Mineradio 有两种用途**

- **内部标识符**（函数名如 `MineradioNormalizeInstallDir`、变量名如 `$MineradioWelcomePage`、define 如 `MINERADIO_INSTALL_MARKER`）→ **不能改**，改了 NSIS 编译报错或运行时找不到函数
- **显示文字和安装路径**（如 `"Mineradio 安装"`、`"C:\Mineradio"`、MessageBox 提示文字）→ **必须改**为 TwT

区分方法：带 `$` 或 `Function` 或 `Call` 或 `Var` 或 `!define` 前缀的是内部标识符；在引号内且出现在 MessageBox / NSD_CreateLabel / DetailPrint / StrCpy $INSTDIR 中的是显示文字或路径。

### 2.4 托盘图标

**坑 8：托盘图标优先从 desktop/assets/ 加载**

`main.js` 中图标候选路径按顺序查找：
```javascript
const APP_ICON_CANDIDATES = [
  path.join(__dirname, 'assets', 'icon.ico'),   // 优先级最高
  path.join(__dirname, 'assets', 'icon.png'),
  path.join(__dirname, '..', 'build', 'icon.ico'),
  path.join(__dirname, '..', 'build', 'icon.png'),
];
```

只改 `build/icon.ico` 不够！`desktop/assets/icon.ico` 优先级更高，会覆盖你的新图标。必须同时替换 `desktop/assets/icon.ico` 和 `desktop/assets/icon.png`。

### 2.5 图标格式转换

源图标是 JPG，需转换为 ICO（多尺寸）和 PNG：

```python
from PIL import Image
import shutil

# JPG → ICO（多尺寸，用于 exe 和托盘）
img = Image.open('图标.jpg').convert('RGBA')
sizes = [(16,16),(32,32),(48,48),(64,64),(128,128),(256,256)]
img.save('build/icon.ico', format='ICO', sizes=sizes)

# 复制到 desktop/assets/
shutil.copy2('build/icon.ico', 'desktop/assets/icon.ico')

# JPG → PNG（256x256，用于托盘备选）
img.resize((256,256), Image.LANCZOS).save('desktop/assets/icon.png', 'PNG')
img.resize((256,256), Image.LANCZOS).save('build/icon.png', 'PNG')
```

## 三、开屏 Logo 动画

### 3.1 设计思路

开屏 logo 改为 **TwTgloomWane**：
- **TwT** — 哭颜文字，T 的横杠先出现，竖线缓慢下落成泪痕
- **gloomWane** — 从右模糊滑入（gloom=阴郁，Wane=消逝）

### 3.2 CSS 动画实现

**坑 9：position:absolute 居中导致文字重叠**

两段 logo 用 `position:absolute` + transform 居中时，文字宽度变化会导致重叠。改用 `position:relative` + flex 布局可根治。

**坑 10：background-clip:text 渐变文字的 descender 截断**

`background-clip: text` 渐变文字中，字母 g 的下伸尾巴（descender）可能超出 line-height 区域导致不着色。需加 `padding-bottom: .18em` 扩展 background 区域。

**坑 11：clip-path 动画需分阶段**

T 的动画分两阶段：
1. 横杠先显示：`clip-path: inset(0 0 86% 0)` → `inset(0 0 0 0)`
2. 竖线延迟下落：800ms 延迟后，4200ms 缓落动画

## 四、打包构建

### 4.1 清理旧产物

```bash
Remove-Item -Recurse -Force dist\
```

### 4.2 执行打包

```bash
$env:ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
$env:ELECTRON_BUILDER_BINARIES_MIRROR="https://npmmirror.com/mirrors/electron-builder-binaries/"
npm run build:win
```

### 4.3 产物

```
dist\TwT-1.1.5-Setup.exe    # NSIS 安装包（~127 MB）
```

## 五、运行时注意事项

**坑 12：ELECTRON_RUN_AS_NODE 环境变量**

如果环境变量中设置了 `ELECTRON_RUN_AS_NODE=1`，electron 会以纯 Node.js 模式运行（无 GUI API），应用窗口不会出现。开发调试时容易误设。

**解决：** 启动前清除：
```bash
Remove-Item Env:ELECTRON_RUN_AS_NODE -ErrorAction SilentlyContinue
```

## 六、完整改动清单

| 文件 | 改动内容 |
|------|----------|
| `package.json` | name, productName, appId, executableName, shortcutName, artifactName, signAndEditExecutable, signExecutable |
| `desktop/main.js` | APP_NAME, APP_USER_MODEL_ID, 13处显示文字 |
| `public/index.html` | title, 开屏logo动画, 7处显示文字 |
| `public/wallpaper.html` | title |
| `public/desktop-lyrics.html` | title |
| `server.js` | 18处显示文字 |
| `build/installer.nsh` | 安装界面文字, 默认安装路径, MessageBox提示 |
| `build/icon.ico` | 从 图标.jpg 转换（多尺寸ICO） |
| `build/icon.png` | 从 图标.jpg 转换（256x256 PNG） |
| `desktop/assets/icon.ico` | 复制自 build/icon.ico |
| `desktop/assets/icon.png` | 从 图标.jpg 转换（256x256 PNG） |