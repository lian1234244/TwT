# TwT 好友功能 & 一起听功能设计方案

## 一、整体架构

```
┌─────────────────┐     WebRTC DataChannel (P2P)     ┌─────────────────┐
│   TwT 客户端 A   │◄──────────────────────────────────►│   TwT 客户端 B   │
│  (你的电脑)      │                                    │  (好友电脑)      │
└────────┬────────┘                                    └────────┬────────┘
         │                                                      │
         │  WebSocket (信令)                                     │  WebSocket (信令)
         ▼                                                      ▼
┌─────────────────────────────────────────────────────────────────────────┐
│                    信令中继服务器 (WebSocket Relay)                        │
│                    用于：好友上下线、房间创建、SDP 交换                    │
│                    技术：Node.js + ws 库，部署在 Render/Railway 免费 tier  │
└─────────────────────────────────────────────────────────────────────────┘
```

### 核心设计原则
- **P2P 优先**：一起听的实时同步走 WebRTC DataChannel，不经过服务器中转，延迟最低
- **信令最轻**：服务器只做好友上下线通知和 WebRTC SDP/ICE 交换，不转发音乐数据
- **无中心账号体系**：用户 ID = �= 邀请码（6位短码），不绑定手机/邮箱
- **跨平台音乐源**：一起听时只同步歌曲元数据（歌名+歌手+平台），各端自行匹配本机源播放

---

## 二、好友功能设计

### 2.1 用户身份

```javascript
// 每个 TwT 实例首次启动时生成唯一 ID
// 存储在 localStorage + userData/friends.json
{
  "myId": "twt_u_x1a2b3c",        // 随机生成，不可变
  "myName": "诗诗",                // 用户昵称，可改
  "myAvatar": "data:image/...",    // 头像（本地图片转 base64）
  "friendCode": "A7K3M9",          // 6位好友码，可重新生成
}
```

### 2.2 好友关系存储

```javascript
// userData/friends.json
{
  "friends": [
    {
      "id": "twt_u9x8y7z",
      "name": "大笨猪",
      "avatar": "data:image/...",
      "friendCode": "B2N5P8",
      "status": "offline",         // online | offline | listening
      "currentSong": null,         // { name, artist, platform } 当 status=listening
      "addedAt": 16930000000008,
      "lastSeen": 1693000000008
    }
  ],
  "pendingRequests": [
    { "from": "twt_u...", "name": "...", "avatar": "...", "timestamp": ... }
  ]
}
```

### 2.3 添加好友流程

```
用户A 输入好友码 B2N5P8
    │
    ▼
客户端A ──ws──► 信令服务器 ──ws──► 客户端B
    "friendRequest { from: A, name: '诗诗', avatar: ... }"
    │
    ▼
客户端B 弹窗："诗诗 请求添加你为好友"
    │
    ├─ 接受 ──ws──► 信令服务器 ──ws──► 客户端A
    │   "friendAccept { from: B, name: '大笨猪', avatar: ... }"
    │   → 双方互存好友关系，开始 WebRTC 连接
    │
    └─ 拒绝 ──ws──► 信令服务器 ──ws──► 客户端A
        "friendReject { from: B }"
```

### 2.4 好友列表 UI

```
┌─────────────────────────────┐
│  好友                    [+] │
├─────────────────────────────┤
│  🟢 大笨猪                  │
│     正在听 《晴天》周杰伦     │
│     [一起听] [发消息]        │
├─────────────────────────────┤
│  🟡 诗诗                    │
│     在线                    │
│     [一起听] [发消息]        │
├─────────────────────────────┤
│  ⚪ 小明                    │
│     离线 · 3小时前           │
└─────────────────────────────┘
```

### 2.5 状态同步

好友上线时通过 WebRTC DataChannel 广播状态：
```javascript
// 状态消息类型
{ type: 'status', status: 'listening', song: { name: '晴天', artist: '周杰伦', platform: 'netease' } }
{ type: 'status', status: 'online' }
{ type: 'status', status: 'offline' }  // 通过信令服务器转发（P2P已断开）
```

---

## 三、一起听功能设计

### 3.1 房间8机制

```
用户A 点击好友B的 [一起听]
    │
    ▼
A 创建房间 ──WebRTC──► B 收到邀请
    "listenTogetherInvite { roomCode: 'TWT808', host: A, song: {当前歌曲} }"
    │
    ├─ B 接受 → 进入同步模式
    │
    └─ B 拒绝 → A 收到拒绝通知
```

### 3.2 同步协议（WebRTC DataChannel 消息）

```javascript
// 1. 房主广播当前播放状态
{ type: 'lt_state', song: { name, artist, platform, duration, cover },
  currentTime: 123.4, playing: true, timestamp: 1693000000008 }

// 2. 播放/暂停同步
{ type: 'lt_play', timestamp: 1693000000008 }
{ type: 'lt_pause', currentTime: 45.2, timestamp: 1693000000008 }

// 3. 切歌同步
{ type: 'lt_switch', song: { name, artist, platform }, timestamp: 1693000000008 }

// 4. 进度同步（每 5 秒一次校正）
{ type: 'lt_sync', currentTime: 89.3, timestamp: 1693000000008 }

// 5. 聊天消息
{ type: 'lt_chat', text: '这首歌好好听', timestamp: 1693000000008 }

// 6. 离开房间
{ type: 'lt_leave', reason: 'user_left' }
```

### 3.3 延迟补偿算法

```javascript
// 接收方收到同步消息后对齐播放进度
function handleLtSync(msg) {
  var now = Date.now();
  var latency = now - msg.timestamp;  // 网络延迟（ms）
  var targetTime = msg.currentTime + latency / 1000;
  var currentAudioTime = audio.currentTime;
  var diff = Math.abs(targetTime - currentAudioTime);

  if (diff > 2.0) {
    // 偏差超过 2 秒，强制 seek
    audio.currentTime = targetTime;
  } else if (diff > 0.5) {
    // 偏差 0.5~2 秒，微调播放速率
    audio.playbackRate = targetTime > currentAudioTime ? 1.02 : 0.98;
    setTimeout(() => { audio.playbackRate = 1.0; }, 2000);
  }
  // 偏差 < 0.5 秒，不处理
}
```

### 3.4 跨平台歌曲匹配

房主在网易云听《晴天》，好友只有 QQ 音乐：
```javascript
// 房主发送
{ type: 'lt_switch', song: { name: '晴天', artist: '周杰伦', platform: 'netease', id: '186016' } }

// 好友收到后，在自己可用的平台搜索匹配
async function findSongOnLocalPlatform(song) {
  // 1. 先尝试同名平台
  // 2. 如果没有该平台登录，搜索其他平台
  var results = await searchAcrossPlatforms(song.name + ' ' + song.artist);
  if (results.length) {
    playSong(results[0]);  // 播放匹配到的版本
  } else {
    showNotification('无法在本地找到: ' + song.name);
    // 降级：只显示歌词和封面，不播放音频
  }
}
```

### 3.5 一起听 UI

```
┌─────────────────────────────────────────┐
│  🎧 一起听 · 房间 TWT808          [离开] │
├─────────────────────────────────────────┤
│                                         │
│     🎵 晴天 - 周杰伦                     │
│     ▶ 01:23 / 04:29                     │
│     ━━━━━●━━━━━━━━━━━━━━━━              │
│                                         │
│  ┌─────────┐         ┌─────────┐       │
│  │ 🟢 诗诗  │         │ 🟢 大笨猪│       │
│  │  房主    │         │  同步中  │       │
│  └─────────┘         └─────────┘       │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │ 诗诗: 这首歌好好听              │   │
│  │ 大笨猪: 是啊！周杰伦永远的神    │   │
│  │ [输入消息...]            [发送] │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

---

## 四、信令服务器设计（轻量 Node.js）

### 4.1 服务器代码（~100 行）

```javascript
// signaling-server.js — 部署到 Render.com / Railway.app 免费 tier
const WebSocket = require('ws');
const wss = new WebSocket.Server({ port:%PORT% || 8080 });

const clients = new Map(); // friendCode -> ws

wss.on('connection', (ws) => {
  ws.on('message', (data) => {
    const msg = JSON.parse(data);
    switch (msg.type) {
      case 'register':
        clients.set(msg.friendCode, ws);
        ws.friendCode = msg.friendCode;
        break;
      case 'friendRequest':
      case 'friendAccept':
      case 'friendReject':
      case 'webrtc_offer':
      case 'webrtc_answer':
      case 'webrtc_ice':
        const target = clients.get(msg.to);
        if (target && target.readyState === 1) target.send(data);
        break;
      case 'status_broadcast':
        // 广播给所有好友
        msg.to.forEach(code => {
          const c = clients.get(code);
          if (c && c.readyState === 1) c.send(data);
        });
        break;
    }
  });
  ws.on('close', () => {
    if (ws.friendCode) clients.delete(ws.friendCode);
  });
});
```

### 4.2 客户端信令连接（嵌入 TwT）

```javascript
// desktop/friends-manager.js
class FriendsManager {
  constructor() {
    this.ws = null;
    this.pc = null;        // RTCPeerConnection
    this.dataChannel = null;
    this.friends = [];
  }

  connectSignaling() {
    this.ws = new WebSocket('wss://twt-signaling.onrender.com');
    this.ws.on('open', () => {
      this.ws.send(JSON.stringify({
        type: 'register',
        friendCode: this.myFriendCode
      }));
    });
    this.ws.on('message', (data) => this.handleSignaling(JSON.parse(data)));
  }

  createDataChannel(friendCode) {
    this.pc = new RTCPeerConnection({
      iceServers: [
        { urls: 'stun:stun.l.google.com:19302' },
        // 免费 STUN 服务器，NAT 穿透
      ]
    });
    this.dataChannel = this.pc.createDataChannel('twt-sync', {
      ordered: true,
      maxRetransmits: 3
    });
    // ... SDP 交换通过信令服务器
  }
}
```

---

## 五、实现优先级

### 第一阶段：好友基础（~2天工作量）
1. 生成用户 ID 和好友码
2. 连接信令服务器
3. 添加好友（输入好友码 → 请求 → 接受/拒绝）
4. 好友列表 UI（侧边栏面板）
5. 上下线状态同步

### 第二阶段：一起听核心（~3天工作量）
6. WebRTC DataChannel 建立
7. 一起听房间创建/加入/离开
8. 播放状态同步（play/pause/switch）
9. 延迟补偿算法
10. 跨平台歌曲匹配

### 第三阶段：体验完善（~2天工作量）
11. 一起听聊天
12. 好2好友正在听的歌曲展示
13. 多人房间（2人→4人）
14. 房主权限控制（只有房主能切歌/控制C暂停）

---

## 六、技术选型总结

| 组件 | 技术 | 理由 |
|------|------|------|
| 信令服务器 | Node.js + ws | ~100行代码，Render 免费部署 |
| P2P 连接 | WebRTC DataChannel | 延迟最低，不中转音乐数据 |
| NAT 穿透 | Google STUN | 免费，覆盖大多数 NAT 场景 |
| 好友存储 | 本地 JSON 文件 | 无需数据库，隐私友好 |
| 状态同步 | DataChannel JSON 消息 | 简单轻量，5秒校正一次 |
| 跨平台匹配 | 搜索 API | 各平台搜索同名歌曲 |
| UI | Electron BrowserWindow | 复用现有渲染管线 |

### 不需要的
- ❌ 不需要数据库（好友关系存本地）
- ❌ 不需要账号体系（好友码即身份）
- ❌ 不需要音乐数据中转（P2P 只传元数据）
- ❌ 不需要付费服务器（Render 免费 tier 够用）